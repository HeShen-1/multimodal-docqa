# 轻量 QLoRA / SFT 训练依赖
# 建议在 Linux / WSL + CUDA 环境安装，Windows 原生 bitsandbytes 兼容性较弱。
# 若切到 `Qwen/Qwen3.5-4B` 或 `Qwen/Qwen3.5-9B`，通常还需要更新到更高版本的 `transformers`
# （官方模型卡建议使用最新 Transformers）。
-r requirements.txt
accelerate>=1.0.1,<1.3
datasets>=2.21.0,<3.0
peft>=0.13.2,<0.16
trl>=0.11.4,<0.13
bitsandbytes>=0.43.3,<0.46
